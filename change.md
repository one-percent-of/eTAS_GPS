1. tab.records
    * ng-click="~~
2. controller = records
    *    $scope.disableLoading = function () {
    return keepGoing;
    }
    *    $scope.pathx = function (id) {
        Records.setId(id);
        };
3. 